# R1.7-14 QA Check (DoD + AC Traceability)

Story: **R1.7-14 – Vergleichswerte (Ø/Best) backendseitig zentral berechnen (Overview + Peak + Segment)**

## Umsetzung / AC-Abdeckung
- Zentraler Backend-Service `SessionComparisonService` liefert Vergleichswerte für Overview + Peak inkl. Segmentkontext.
- Vergleichs-Scope ist session-typbasiert; Basis sind die zuletzt datierten Sessions bis `comparisonSessionsCount`.
- Profil enthält `comparisonSessionsCount` (Default 5, validiert 1..20) inkl. Persistenz und API-Feldern.
- API liefert `averageLastN` und `best` sowie Availability-Flags/Reason pro Metrik.
- Frontend nutzt serverseitige Vergleichswerte (mit Fallback für Legacy/Testdaten).

## DoD Review (Story-relevant)
- Funktionalität: **erfüllt** (zentralisierte Vergleichswerte über Overview/Peak/Segment-Scope).
- Qualität: **erfüllt** (Backend- und Frontend-Tests grün).
- Dokumentation: **erfüllt** (Story auf done gesetzt, QA-Check ergänzt).

## Test-Run
- `dotnet test backend/tests/FootballMetrics.Api.Tests/FootballMetrics.Api.Tests.csproj`
- `npm test` (frontend)
- `npm run build` (frontend)

Aus QA-Sicht ist R1.7-14 im Story-Scope **Done-fähig**.
